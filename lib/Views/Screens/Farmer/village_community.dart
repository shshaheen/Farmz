import 'package:flutter/material.dart';

class VillageCommunity extends StatelessWidget {
  const VillageCommunity({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Village Community')
        ),
    );
  }
}