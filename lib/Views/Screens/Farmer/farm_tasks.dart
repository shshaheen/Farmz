import 'package:flutter/material.dart';

class FarmTasks extends StatelessWidget {
  const FarmTasks({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Add Tasks here..!"),
      ),
    );
  }
}